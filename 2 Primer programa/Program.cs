using System;

namespace Ejercicios2
{
    class Program
    {
        static void Main(string[] args)
        {//programa 1:
            // int n1, n2, resultado;
            // Console.WriteLine("Ingrese un numero: ");
            // n1 = int.Parse(Console.ReadLine());
            // Console.WriteLine("Ingrese otro numero: ");
            // n2 = int.Parse(Console.ReadLine());
            // resultado = n1 + n2;
            // Console.WriteLine(resultado);

            //Programa 2:
            // int n1, cubo;
            // Console.WriteLine("Ingrese un numero:");
            // n1=int.Parse(Console.ReadLine());
            // cubo=n1*n1*n1;
            // Console.WriteLine(cubo);

            //Programa 3: Hacer un programa que permita ingresar los 
            // kilómetros existentes entre dos ciudades y la velocidad promedio
            // de un vehículo. Calcular y emitir por pantalla el tiempo aproximado que 
            // demandará llegar de un punto a otro teniendo en cuenta los datos ingresados.

            // float n1, n2; 
            // float tiempo;
            // Console.WriteLine("Ingrese la distancia en km entre las 2 ciudades: ");
            // n1=float.Parse(Console.ReadLine());
            // Console.WriteLine("Ingrese la velocidad promedio  en Km/hr a la que esta viajando el vehiculo: ");
            // n2=float.Parse(Console.ReadLine());
            // tiempo= n1/n2;
            // Console.WriteLine(" Se demora " + tiempo + " Horas en llegar a su destino");
    

            // Programa 4: Una casa de computación paga a sus empleados un sueldo fijo 
            // de ARS15000 más una comisión del 5% sobre el total facturado por cada empleado. 
            // Hacer un programa para ingresar el total facturado por un empleado y que luego 
            // calcule y emita por pantalla el sueldo total a cobrar por el mismo.

            // float facturado, comision, sueldoFinal;
            // Console.WriteLine("Ingrese el total facturado por el empleado: ");
            // facturado = float.Parse(Console.ReadLine());
            // comision = (float)(facturado * 0.05);
            // sueldoFinal = 15000 + comision;
            // Console.WriteLine("El sueldo final del empleado es: " + sueldoFinal);


            // 5. Hacer un programa para ingresar por teclado las tres notas de exámenes de un alumno y 
            // luego calcule y emita por pantalla el promedio final.

            // float nota1, nota2, nota3, promedio;
            // Console.WriteLine("Ingrese la nota 1: ");
            // nota1 = float.Parse(Console.ReadLine());
            // Console.WriteLine("Ingrese la nota 2: ");
            // nota2 = float.Parse(Console.ReadLine());
            // Console.WriteLine("Ingrese la nota 3: ");
            // nota3 = float.Parse(Console.ReadLine());
            // promedio = (nota1 + nota2 + nota3)/3;
            // Console.WriteLine("El promedio de notas es: " + promedio);


        







        }
    }
}
